# Auto-provision the Prometheus datasource so the stack comes up wired, with no
# manual clicking. `prometheus` is the compose service name on the shared network.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false